import { test, expect } from '@playwright/test';

test.describe('all testcases for testing', () =>{

    test('Navigate to page', async({page}) => {
 
        
    await page.goto('https://www.multidots.com/');
    });













});